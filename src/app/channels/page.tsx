import {channels} from "@/lib/channels";export default function Channels(){return <main className="mx-auto min-h-screen max-w-7xl px-4 py-8"><a href="/" className="text-green-400">← CricketHub</a><h1 className="mt-8 text-4xl font-black">📺 All Channels</h1><p className="mt-2 text-slate-400">Select a configured channel.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{channels.map(c=><a key={c.id} href={"/channels/"+c.id} className="card rounded-3xl p-6"><div className="flex justify-between"><span className="text-3xl">📺</span><span className={c.url?"text-xs text-green-400":"text-xs text-slate-500"}>{c.url?"READY":"NOT CONFIGURED"}</span></div><h2 className="mt-5 text-xl font-black">{c.channel_name}</h2><p className="mt-2 text-sm text-slate-400">{c.url?"Open player":"Configure URL in src/lib/channels.ts"}</p></a>)}</div></main>}