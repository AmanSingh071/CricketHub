import {getCurrentMatches}from"@/lib/cricket";import MatchesExplorer from"@/components/MatchesExplorer";export const dynamic="force-dynamic";export default async function Matches(){const matches=await getCurrentMatches();return <main className="mx-auto min-h-screen max-w-7xl px-4 py-8"><p className="text-xs font-black tracking-[.2em] text-sky-400">MATCH CENTER</p><h1 className="mt-2 text-4xl font-black">All Cricket Matches</h1><p className="mt-3 text-slate-400">Filter live, upcoming and completed matches from the current CricketHub data feed.</p><MatchesExplorer matches={matches}/></main>}