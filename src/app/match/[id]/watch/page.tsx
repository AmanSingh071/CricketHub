export default async function WatchPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const source=process.env.WATCH_EMBED_URL?.replace("{matchId}",encodeURIComponent(id));return <main className="mx-auto min-h-screen max-w-6xl px-4 py-8"><a href={"/match/"+id} className="text-green-400">← Match Center</a><h1 className="mt-8 text-4xl font-black">Watch Center</h1><p className="mt-2 text-sm text-slate-400">Player area for an official or otherwise authorized embed source.</p><div className="card mt-8 overflow-hidden rounded-3xl bg-black">{source?<iframe title="Authorized cricket stream" src={source} className="aspect-video w-full border-0" allow="autoplay; fullscreen; picture-in-picture" allowFullScreen/>:<div className="flex aspect-video items-center justify-center p-8 text-center"><div><div className="text-5xl">📺</div><p className="mt-5 text-xl font-bold">Video source not configured</p><p className="mt-2 max-w-xl text-sm text-slate-400">Add WATCH_EMBED_URL in Vercel. You can use a matchId placeholder in an authorized provider URL.</p></div></div>}</div></main>}